// ctr+d is EOF

# include <stdio.h>

int main() {
	

/*
	// 1.5.3 Line Counting
	int c, nl;
	
	nl = 0;
	while((c = getchar()) != EOF) {
		if(c == '\n' || c == ' ' || c == '\t')
			++nl;
	}
	printf("%d\n", nl);
*/

/*	
	// 1.5.2 Character Counting	
	double nc; 

	for(nc = 0; getchar() != EOF; ++nc)
	printf("%.0f\n", nc);
*/

/*
	// 1.5.1 - File Copying
	int c; 
	while((c = getchar() != EOF)) {
		putchar(c);

		printf("%d\n", c);
	}

	printf("%d\n", c);
	return 0;
*/
}
